{
  "name": "ocpp-log-report",
  "version": "1.0.0",
  "description": "Client-side OCPP 1.6J log parser — upload a CSV in the browser and get per-transaction graphs and a fault/voltage summary.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "papaparse": "^5.4.1",
    "plotly.js-dist-min": "^2.35.2"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
