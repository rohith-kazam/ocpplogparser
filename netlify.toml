# Netlify deployment config for the OCPP log report app
[build]
  command = "npm run build"
  publish = "dist"

# SPA-style fallback (single page, no server routes needed)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
